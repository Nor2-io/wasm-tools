package foo:resources1

interface resources1 {
  t1: func(a: rc<r1>) -> ()

  resource r1 {
    //TODO: Fix so it will allow references to itself if it's inside a handle
    //f1: func() -> (a: s32, handle: rc<r1>)
  }
}

